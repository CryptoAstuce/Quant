# Quant currently requires no secrets. The MVP uses the public HyperEVM RPC
# configured in src/lib/chain.ts. Add future public Vite variables here and
# never place private keys, seed phrases, or server-only secrets in VITE_* vars.
